import React from 'react';
import ReactDOM from 'react';

document.addEventListener("DOMContentLoaded", () => {
  const root = document.getElementById("root")
  ReactDOM.render(<h1>Where Saving the World Happens</h1>, root)
});
